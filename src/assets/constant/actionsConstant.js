/**
 * @Description 用于定义redux的actions名称，如不懂redux，可暂时跳过。这里的常量定义只是为了actions名字不能重复，用于记录下actions名字，防止协同开发时，重复定义相同名称。
 */

const actionsConstant = {
    FETCH_START: "FETCH_START", // 请求开始
    FETCH_END: "FETCH_END", // 请求结束
}

export default actionsConstant